FROM node:18-alpine

# устанавливаем директорию внутри контейнера, где будут выполняться все команды
WORKDIR /app

# Копируем все файлы проекта в контейнер
COPY . .

# устанавливаем порт
EXPOSE 8098

# запуск веб-сервера через Node.js при запуске
CMD ["node", "forfront.js"]